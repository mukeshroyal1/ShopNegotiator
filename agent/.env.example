DJANGO_API_URL=http://127.0.0.1:8000/api
AGENT_SERVICE_SECRET=dev-agent-secret-change-me

# Required for natural phone dialogue (Twilio → Django → agent → OpenAI)
OPENAI_API_KEY=
# Optional; default is fast/cheap for live calls
OPENAI_MODEL=gpt-4o-mini

# Fair-price ML FastAPI (cd ml && uvicorn app.main:app --port 8090)
ML_SERVICE_URL=http://127.0.0.1:8090
